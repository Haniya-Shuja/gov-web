import React from 'react';

import Footer from '@/app/components/Footer/Footer'; // Adjust the path if Footer is in a different folder

const Job = () => {
  return (
    <section>
      <div className="text-center font-bold">Job/Internship Coming Soon</div>
      <Footer /> {/* Footer component */}
    </section>
  );
};

export default Job;
